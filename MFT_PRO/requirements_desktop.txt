# MFT System - Desktop Launcher Requirements
# Install with: pip install -r requirements_desktop.txt

# Core Flask and web dependencies (already installed)
flask>=2.0.0
werkzeug>=2.0.0

# Desktop window support
pywebview>=4.0.0

# System tray support (optional but recommended)
pystray>=0.19.0
pillow>=9.0.0

# Note: pywebview may require additional system dependencies:
# - On Linux: install python3-gi python3-gi-cairo gir1.2-gtk-3.0 gir1.2-webkit2-4.0
# - On Windows: No additional dependencies needed
# - On macOS: No additional dependencies needed
